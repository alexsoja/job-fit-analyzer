# ApplicationCalculator
A project that takes a user's Resume and matches it to a given job description. The tool should provide an accuracy rating to show confidence if the user should apply for the position or not, and why.
